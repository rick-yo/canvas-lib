{
  "name": "canvas-lib",
  "version": "0.0.3",
  "description": "canvas library with a delightful API",
  "main": "src/index.ts",
  "typings": "dist/types/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel example/index.html --open",
    "build": "tsc --module commonjs",
    "prebuild": "rm -rf dist types",
    "site": "typedoc --excludeNotExported --excludePrivate --excludeProtected --readme README.MD --theme minimal --out docs --target es6 --theme minimal --mode file src"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/luv-sic/canvas-lib.git"
  },
  "keywords": [
    "canvas",
    "library",
    "util"
  ],
  "author": "https://github.com/luv-sic",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/luv-sic/canvas-lib/issues"
  },
  "homepage": "https://github.com/luv-sic/canvas-lib#readme",
  "dependencies": {
    "lodash": "^4.17.11"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.135",
    "typedoc": "^0.14.2",
    "typescript": "^3.5.2"
  }
}
